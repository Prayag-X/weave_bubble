import 'package:flutter/material.dart';

import 'emotion.dart';

class CalmEmotion extends Emotion {
  const CalmEmotion({
    super.primaryColor = Colors.transparent,
    super.secondaryColor = Colors.blue,
  });
}